name: "CodeQL Analysis OSAL Default Build"

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  codeql:
    name: CodeQl Analysis
    uses: jphickey/cFS/.github/workflows/codeql-reusable.yml@fix-998-workflow-config
    with:
      component-path: osal
      setup: 'cp Makefile.sample Makefile'
      make: 'make'
